h1:Usage - Demo
<?pre 
docker run -p 80:80 mywise:latest
#or
docker run -p 80:80 -name m2 mywise:latest  #name as 'm2'
?>
h1:Usage - Download to Local and Run from Local
h2:Download Site Files
<?pre
docker run -d -p 80:80 --name m2 mywise:latest
docker cp m2:/site-files .
?>
h2:Run the local Site Files
<?pre
# assume local directory has a subdirectory called 'site-files' 
docker run -p 80:80 -v `pwd`/site-files:/site-files --name m3 mywise:latest
?>
